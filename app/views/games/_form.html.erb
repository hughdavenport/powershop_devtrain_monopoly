<%= form_for(@game) do |f| %>
  <% if @service && @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number_of_players %><br>
    <%= f.text_field :number_of_players %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
